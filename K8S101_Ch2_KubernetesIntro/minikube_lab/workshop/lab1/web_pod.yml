apiVersion: v1
kind: Pod

metadata:
  name: web
  labels:
    name: web
    app: demo

spec:
  containers:
  - name: web
    image: "web:po"
    env:
    - name: "REDIS_HOST"
      value: "172.17.0.8"
    ports:
    - containerPort: 80
